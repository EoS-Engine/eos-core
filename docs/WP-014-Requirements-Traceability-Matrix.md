# WP-014 Requirements Traceability Matrix — Memory: Full Retrieval Strategy & Mechanical Ranking

**Purpose:** Maps every requirement stated in the WP-014 row of `docs/EOS-Implementation-Roadmap-v1.0.md` to its governing specification section(s), its delivered implementation status, and the concrete code/test evidence for that status. Intended as a standing reference for future architecture audits of this Work Package.

**Related documents:** `docs/Memory-Management-Specification-v1.0.md`, `docs/Learning-Engine-Specification-v1.1.md`, `docs/EOS-Specification.md`, `docs/Architecture-Gaps/AG-0001-WP014-Hybrid-Retrieval-Inconsistency.md`.

---

| Roadmap Requirement | Specification Reference | Implementation Status | Evidence (code/tests) | Notes |
|---|---|---|---|---|
| "Implement the full seven memory-type Storage Strategy" | Memory-Management-Specification-v1.0 §10 (Memory Types), §12 (Storage Strategy table) | Complete | `src/EOS.Knowledge/MemoryType.cs` (7-value enum); `src/EOS.Knowledge/KnowledgeClient.cs`'s `ResolveNodeTypes(MemoryType?)`; `src/EOS.Infrastructure/RedisMemoryStore.cs`. Tests: `tests/EOS.Knowledge.Tests/KnowledgeClientQueryTests.cs` (Episodic/Semantic/LongTerm/Project mapping); `tests/EOS.Infrastructure.Tests/RedisMemoryStoreTests.cs` (Working/Short-term/Session backing store). | All seven `MemoryType` values map to a defined Part 4 store per §12's table. |
| "...hybrid symbolic+vector Retrieval Strategy..." — symbolic half | Memory-Management-Specification-v1.0 §13 stage 1 | Complete | `src/EOS.KnowledgeGraph/KnowledgeGraphStore.cs`'s `QueryAsync(nodeTypes, createdFrom, createdTo, ...)`; `src/EOS.Knowledge/KnowledgeClient.cs`'s `QueryAsync`/`QuerySimilarAsync`. Tests: `tests/EOS.Knowledge.Tests/KnowledgeClientQueryTests.cs`. | §13: *"Symbolic stage: `EOS.KnowledgeGraph` resolves an initial candidate set via structured filters (memory type, `domain_tags`, node type per §0.5.1, time range)."* Matches `query()`'s three parameters exactly. |
| "...hybrid symbolic+vector Retrieval Strategy..." — vector half | Memory-Management-Specification-v1.0 §13 stage 2, §9 (component diagram), §23.2 (sequence diagram) | Outside WP Scope | None (no `EOS.VectorStore` production code exists; scaffold project unchanged from pre-WP-014 state). | See AG-0001. §9 wires `EOS.VectorStore` only to `ContextAssembler`/`LifecycleEngine`, never to the components associated with `query()`/`query_similar()`. §23.2 shows the vector-similarity call occurring inside `assemble_context()` (§15/WP-015), after and separate from a `query()` call that itself returns only "symbolic candidates." |
| "...and the mechanical Retrieval Ranking formula" | Memory-Management-Specification-v1.0 §19 (Memory Retrieval Ranking), §19.1 (Ranking Formula) | Complete | `src/EOS.Knowledge/RetrievalRanking.cs`; `src/EOS.Knowledge/RankingWeights.cs`; `src/EOS.SharedKernel/Configuration/ThresholdsOptions.cs` (four weight fields); `config/Thresholds.json`. Tests: `tests/EOS.Knowledge.Tests/RetrievalRankingTests.cs`. | Formula's four terms (`vector_similarity`, `recency_decay`, `domain_match`, `access_frequency`) are all structurally present per §19.1. `recency_decay`/`domain_match` are populated with real data; `vector_similarity`/`access_frequency` evaluate to zero — disclosed in `RetrievalRanking.cs`'s doc comment (no upstream data source exists for either yet). |
| Prerequisites: "WP-007" | Memory-Management-Specification-v1.0 §20.1 (`IKnowledgeClient`); prior WP-007 Implementation Plan | Complete | `src/EOS.KnowledgeGraph/KnowledgeGraphStore.cs` (WP-007-authored `UpsertAsync`/`GetByIdAsync`, extended this WP with `QueryAsync`). | WP-007's storage layer reused and extended, not replaced. |
| Prerequisites: "WP-011 (embedding channel)" | Memory-Management-Specification-v1.0 §20.2 (`IEmbeddingProviderClient`) | Outside WP Scope | None (`IEmbeddingProviderClient`, `AIProviderManager.EmbedAsync` not referenced anywhere in `EOS.Knowledge`/`EOS.KnowledgeGraph`). | See AG-0001. Neither `query()` nor `query_similar()`'s ratified signature (§20.1) contains a field that could supply content to embed. |
| Included components: "the two-stage symbolic+vector retrieval algorithm" | Memory-Management-Specification-v1.0 §13 | Partially covered — symbolic stage Complete, vector stage Outside WP Scope | See the two "hybrid" rows above. | Same finding, restated at the "Included components" wording level per AG-0001. |
| Included components: "the four-weight mechanical ranking formula" | Memory-Management-Specification-v1.0 §19.1 | Complete | See ranking-formula row above. | |
| Included components: "All seven memory types... mapped to their Constitution Part 4 stores (Redis, SQLite, SQL Server, ChromaDB via `EOS.VectorStore`)" | Memory-Management-Specification-v1.0 §12; Constitution Part 4 §4.1 | Complete for Redis/SQL Server rows; Not Applicable for SQLite/ChromaDB rows | `RedisMemoryStore.cs` (Redis); `KnowledgeGraphStore.cs` (SQL Server). | SQLite backing (§12: "Session... SQLite (offline/mobile)") is explicitly Mobile-domain and out of this 30-WP roadmap's scope (roadmap's own Mobile exclusion note). ChromaDB/`EOS.VectorStore` backing for Semantic/Long-term is Outside WP Scope per the vector-retrieval finding above — the store mapping is declared in §12 but no read/write code against it exists this WP. |
| Explicitly excluded: "Context Assembly's budget/truncation logic (WP-015)" | Memory-Management-Specification-v1.0 §15 | Deferred by Specification | None — confirmed absent: no `assemble_context`, `ContextPayload`, or `ContextRequest` type exists anywhere in the codebase. | Correctly not built; roadmap-mandated exclusion honored. |
| Explicitly excluded: "Consolidation/Compression/Expiration (WP-015/WP-016)" | Memory-Management-Specification-v1.0 §16 (Consolidation), §17 (Compression), §18 (Expiration) | Deferred by Specification | None — confirmed absent: no `consolidate`, `EpisodicEntryRef`, or compression/expiration sweep code exists anywhere in the codebase. | Correctly not built; roadmap-mandated exclusion honored. |
| Expected deliverables: "`IKnowledgeClient.query()`/`.query_similar()` return real, ranked results against real stored content" | Memory-Management-Specification-v1.0 §20.1; Learning-Engine-Specification-v1.1 §11.2/§14.3 | Complete | `src/EOS.Knowledge/IKnowledgeClient.cs`; `src/EOS.Knowledge/KnowledgeClient.cs`. Tests: `tests/EOS.Knowledge.Tests/KnowledgeClientQueryTests.cs` (all `QueryAsync`/`QuerySimilarAsync` scenarios against real SQL Server). | `query_similar()` returns a real symbolic candidate pool (same `NodeType`, excluding self), matching Learning-Engine-Specification-v1.1 §11.2's actual consumption (`candidates = Knowledge.query_similar(...)`, with similarity computed afterward by `ReasoningEngine.compare()`, per §14.3 and Memory-Management-Specification §5's Non-Responsibilities). |
| Demo/acceptance criteria: "A seeded set of test knowledge items returns in the expected rank order for a sample query" | Memory-Management-Specification-v1.0 §19.1 (formula), §13 (retrieval) | Complete | `tests/EOS.Knowledge.Tests/RetrievalRankingTests.cs` (deterministic formula-order assertions); `tests/EOS.Knowledge.Tests/KnowledgeClientQueryTests.cs` (real seeded SQL Server data, verified retrieval/order). | |
| `MemoryType`↔`KnowledgeNodeType` mapping (G3, Architecture Challenge) | Memory-Management-Specification-v1.0 §10.3 (Long-term), §10.4 (Episodic), §10.5 (Semantic), §10.6 (Project) | Complete | `src/EOS.Knowledge/KnowledgeClient.cs`'s `ResolveNodeTypes`. Tests: `tests/EOS.Knowledge.Tests/KnowledgeClientQueryTests.cs` (`QueryAsync_ReturnsOnlyLessonNodes_ForEpisodicMemoryType`, `..._ForSemanticMemoryType`, `..._ForLongTermMemoryType`, `..._ForProjectMemoryType`). | `Episodic→Lesson`; `Semantic→{Fact,Pattern}`; `LongTerm→{Fact,Pattern,Decision,Risk}`; `Project→domain_tags` filter over all types. No `KnowledgeNodeType` enum change. |
| `query()`'s exclusion of ephemeral `MemoryType`s | Memory-Management-Specification-v1.0 §10.1 (Working), §10.2 (Short-term), §10.7 (Session) | Complete (documented architectural limitation) | `src/EOS.Knowledge/KnowledgeClient.cs`'s `ResolveNodeTypes` (throws `NotSupportedException` for `Working`/`ShortTerm`/`Session`). Tests: `tests/EOS.Knowledge.Tests/KnowledgeClientQueryTests.cs`'s `QueryAsync_ThrowsNotSupported_ForEphemeralMemoryTypes`. | Working/Short-term/Session content is never a `KnowledgeNode` (§10.1/§10.2/§10.7: "raw, unclassified"), so `query()` — a Knowledge Graph query API — cannot return it. Documented on `IKnowledgeClient.QueryAsync`'s own XML doc comment. |
| Ranking-weight configuration placement | Memory-Management-Specification-v1.0 §19.1 ("Weights (`w1..w4`) are externally configurable (`Thresholds.json`...)") | Complete | `src/EOS.SharedKernel/Configuration/ThresholdsOptions.cs`; `config/Thresholds.json`. | Kept in `ThresholdsOptions` per §19.1's direct, explicit citation of `Thresholds.json` — not moved to `KnowledgeOptions`. |

---

## Summary

- **Complete:** 12 of 15 rows (Storage Strategy, symbolic retrieval, ranking formula, WP-007 prerequisite, `MemoryType`/`NodeType` mapping, ephemeral-type exclusion, ranking-weight placement, expected deliverables, acceptance criteria).
- **Outside WP Scope:** 3 rows, all traced to the same single root cause documented in AG-0001 (vector-retrieval stage assigned to `ContextAssembler`/`assemble_context()`, WP-015): the vector half of "hybrid symbolic+vector Retrieval Strategy," the WP-011 embedding-channel prerequisite, and the ChromaDB portion of the seven-memory-type storage mapping.
- **Deferred by Specification:** 2 rows (Context Assembly, Consolidation/Compression/Expiration) — both are roadmap-mandated exclusions from WP-014, correctly honored.
- **No requirement in this matrix is classified Missing.**

This matrix supersedes no prior document; it consolidates findings already established across the WP-014 Architecture Review, Architecture Challenge, Implementation Plan, and AG-0001 into a single per-requirement reference.
